const initialState = {
    userName: '',
    isAuthenticated: false
}

export function profileReducer(state = initialState, action: any) {
    switch(action.type) {
        default:
            return state;
    }
}